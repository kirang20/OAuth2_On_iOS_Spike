#include "Pods-OAuth2_On_iOS_Spike-JSONAPI.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/JSONAPI" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/JSONAPI"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}